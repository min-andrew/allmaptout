# Database
DB_USER=postgres
DB_PASSWORD=changeme
DB_NAME=wedding
DB_HOST=localhost
DB_PORT=5432
DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Backend
PORT=3001
RUST_LOG=info

# Auth (generate your own secret: openssl rand -base64 32)
JWT_SECRET=changeme
