{
  "type": "object",
  "description": "Response for RSVP status check.",
  "required": ["has_responded", "party_size", "guest_name"],
  "properties": {
    "guest_name": { "type": "string" },
    "has_responded": { "type": "boolean" },
    "party_size": { "type": "integer", "format": "int32" },
    "rsvp": {
      "oneOf": [
        { "type": "null" },
        {
          "type": "object",
          "description": "Response for an RSVP with its attendees.",
          "required": ["id", "guest_id", "responded_at", "attendees"],
          "properties": {
            "attendees": {
              "type": "array",
              "items": {
                "type": "object",
                "description": "Response for a single attendee.",
                "required": ["id", "name", "is_attending", "is_primary"],
                "properties": {
                  "dietary_restrictions": { "type": ["string", "null"] },
                  "id": { "type": "string", "format": "uuid" },
                  "is_attending": { "type": "boolean" },
                  "is_primary": { "type": "boolean" },
                  "meal_preference": { "type": ["string", "null"] },
                  "name": { "type": "string" }
                },
                "x-readme-ref-name": "AttendeeResponse"
              }
            },
            "guest_id": { "type": "string", "format": "uuid" },
            "id": { "type": "string", "format": "uuid" },
            "responded_at": { "type": "string" }
          },
          "x-readme-ref-name": "RsvpResponse"
        }
      ]
    }
  },
  "x-readme-ref-name": "RsvpStatusResponse"
}
